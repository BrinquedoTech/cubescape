import * as THREE from 'three';

const FloorSpikesGeneralConfig = {
  geometryBaseRotation: new THREE.Euler(Math.PI * 0.5, 0, 0),
  geometrySpikeRotation: new THREE.Euler(Math.PI * 0.5, 0, 0),
}

export { FloorSpikesGeneralConfig,  };