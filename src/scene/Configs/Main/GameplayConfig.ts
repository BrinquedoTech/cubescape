import { IGameplayConfig } from "../../Interfaces/IGameplayConfig";

const GameplayConfig: IGameplayConfig = {
  grid: {
    size: 1,
    scale: 1,
  },
  cube: {
    rotationSpeed: 2,
  },
  playerCharacter: {
    speed: 10,
  },
}

export default GameplayConfig;
